trigger:
  branches:
    include:
    - dev

pool:
  name: MyAgentPool
  demands:
  - agent.name -equals MyWindowsAgent

steps:
- script: echo "Hello from Azure Pipeline running on MyWindowsAgent!"
  displayName: 'Test Agent Connectivity'

- script: |
    echo "Building project..."
    echo "Simulating deployment to VM"
  displayName: 'Build and Deploy'
